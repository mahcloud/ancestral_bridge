<p id="notice"><%= notice %></p>

<h1>Listing trees</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Root Person</th>
    <th>Size</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trees.each do |tree| %>
  <tr>
    <td><%= tree.name %></td>
    <td><%= tree.person_id %></td>
    <td><%= tree.people.length %></td>
    <td><%= link_to 'Show', tree %></td>
    <td><%= link_to 'Edit', edit_tree_path(tree) %></td>
    <td><%= link_to 'Destroy', tree, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tree', new_tree_path %>
